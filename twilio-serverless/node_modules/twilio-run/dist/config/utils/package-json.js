"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readPackageJsonContent = void 0;
const path_1 = __importDefault(require("path"));
const fs_1 = require("../../utils/fs");
async function readPackageJsonContent({ cwd, }) {
    if (!cwd) {
        throw new Error('Missing cwd to find package.json');
    }
    const pkgJsonPath = path_1.default.join(cwd, 'package.json');
    if (!(await (0, fs_1.fileExists)(pkgJsonPath))) {
        throw new Error(`Failed to find package.json file at "${pkgJsonPath}"`);
    }
    const pkgContent = await (0, fs_1.readFile)(pkgJsonPath, 'utf8');
    const pkgJson = JSON.parse(pkgContent);
    return pkgJson;
}
exports.readPackageJsonContent = readPackageJsonContent;
